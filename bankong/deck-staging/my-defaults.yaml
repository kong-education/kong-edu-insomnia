_info:
  defaults:
    routes:
      regex_priority: 0
      request_buffering: true
      response_buffering: true
      strip_path: false
    services:
      - connect_timeout: 60000
      enabled: true
      read_timeout: 60000
      retries: 5
